# Profiling helpers taken from
# https://blog.tarkalabs.com/optimize-zsh-fce424fcfd5
# Used along with config in .zshrc
timezsh() {
  shell=${1-$SHELL}
  for i in $(seq 1 10); do /usr/bin/time $shell -i -c exit; done
}
